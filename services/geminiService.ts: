
import { GoogleGenAI, Type, Modality } from "@google/genai";
import { IntakeData, Meal, DailyLog, WeeklyCheckIn, FoodItem, WeeklyReview, LoggedFood } from "../types";

const parseGeminiJson = (text: string) => {
  try {
    const cleaned = text.replace(/```json/g, '').replace(/```/g, '').trim();
    return JSON.parse(cleaned);
  } catch (e) {
    console.error("Failed to parse JSON:", e);
    return null;
  }
};

export async function generatePersonalizedPlan(data: IntakeData): Promise<any> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  const response = await ai.models.generateContent({
    model: "gemini-3-pro-preview",
    contents: `You are a world-class fitness coach for Asian professionals living in the UK. 
    Create a simple 12-week training and eating plan for:
    Name: ${data.name}
    Gender: ${data.gender}
    Main Goal: ${data.goal}
    Dietary Preference: ${data.dietPreference}
    Cultural Background: ${data.culturalPreference}
    Current Weight: ${data.weight}kg, Height: ${data.height}cm
    
    IMPORTANT REQUIREMENTS:
    - Use UK English spelling (Programme, Colour, Analysing).
    - If FEMALE: Focus on tone, strength, and health. No "bulking" language.
    - If MALE: Focus on athletic shape and strength.
    - NUTRITION: Suggest foods available in UK supermarkets (e.g., Tesco, Waitrose). Include healthy Asian staples (Lentils, Tofu, Paneer, Grilled Meats).
    - Keep instructions simple and jargon-free.
    
    Return a JSON object with trainingDayMacros (calories, p, c, f), restDayMacros, mealPlan (array of meals with name, ingredients, instructions), workoutSplit (array of days with exercises), and coachAdvice.`,
    config: {
      responseMimeType: "application/json"
    }
  });
  return parseGeminiJson(response.text);
}

export async function groundedConciergeChat(prompt: string, context: string): Promise<{ text: string, sources: any[] }> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  const response = await ai.models.generateContent({
    model: "gemini-3-pro-preview",
    contents: `Context: ${context}\n\nClient Question: ${prompt}`,
    config: {
      tools: [{ googleSearch: {} }],
      systemInstruction: "You are the MUSKYFIT support coach. Use UK English. Be helpful, clear, and direct. Help the client with their training or diet."
    },
  });
  return { text: response.text || "Checking that for you...", sources: response.candidates?.[0]?.groundingMetadata?.groundingChunks || [] };
}

export async function getMealAlternatives(meal: Meal): Promise<string[]> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  const response = await ai.models.generateContent({
    model: "gemini-3-flash-preview",
    contents: `Suggest 3 simple UK Asian-friendly alternatives for: ${meal.name} with similar nutrition.`,
    config: {
      responseMimeType: "application/json",
      responseSchema: { type: Type.ARRAY, items: { type: Type.STRING } }
    }
  });
  return parseGeminiJson(response.text) || [];
}

export async function analyzeWeeklyPerformanceDeep(logs: DailyLog[], checkIn: WeeklyCheckIn): Promise<any> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  const response = await ai.models.generateContent({
    model: "gemini-3-pro-preview",
    contents: `Review this client's week: Logs: ${JSON.stringify(logs)}, Check-in: ${JSON.stringify(checkIn)}. Keep the feedback simple and encouraging. Use UK English.`,
    config: {
      responseMimeType: "application/json",
      responseSchema: {
        type: Type.OBJECT,
        properties: {
          adherenceScore: { type: Type.NUMBER },
          coachMessage: { type: Type.STRING },
          directives: { type: Type.ARRAY, items: { type: Type.STRING } },
          status: { type: Type.STRING, enum: ["GREEN", "AMBER", "RED"] }
        },
        required: ["adherenceScore", "coachMessage", "directives", "status"]
      }
    }
  });
  return parseGeminiJson(response.text);
}

export async function generateExerciseDemoVideo(exerciseName: string): Promise<string | null> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  let operation = await ai.models.generateVideos({
    model: 'veo-3.1-fast-generate-preview',
    prompt: `Professional video demonstration of a fitness expert performing: ${exerciseName}. Plain background, clear form.`,
    config: { numberOfVideos: 1, resolution: '720p', aspectRatio: '9:16' }
  });
  while (!operation.done) {
    await new Promise(resolve => setTimeout(resolve, 10000));
    operation = await ai.operations.getVideosOperation({ operation: operation });
  }
  const link = operation.response?.generatedVideos?.[0]?.video?.uri;
  return link ? `${link}&key=${process.env.API_KEY}` : null;
}

export async function generateWeeklyAudioBriefing(text: string): Promise<string | undefined> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  const response = await ai.models.generateContent({
    model: "gemini-2.5-flash-preview-tts",
    contents: [{ parts: [{ text }] }],
    config: {
      responseModalities: [Modality.AUDIO],
      speechConfig: { voiceConfig: { prebuiltVoiceConfig: { voiceName: 'Kore' } } },
    },
  });
  return response.candidates?.[0]?.content?.parts?.[0]?.inlineData?.data;
}

// Fix: analyzeMealPhoto for nutritional extraction from images
export async function analyzeMealPhoto(base64: string): Promise<any> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  const imagePart = {
    inlineData: {
      mimeType: 'image/jpeg',
      data: base64,
    },
  };
  const response = await ai.models.generateContent({
    model: 'gemini-3-flash-preview',
    contents: {
      parts: [
        imagePart,
        { text: 'Estimate the nutritional content of the meal in this image. Focus on UK portion sizes. Return JSON.' }
      ]
    },
    config: {
      responseMimeType: "application/json",
      responseSchema: {
        type: Type.OBJECT,
        properties: {
          name: { type: Type.STRING },
          calories: { type: Type.NUMBER },
          protein: { type: Type.NUMBER },
          carbs: { type: Type.NUMBER },
          fats: { type: Type.NUMBER },
          servingSize: { type: Type.STRING }
        },
        required: ["name", "calories", "protein", "carbs", "fats", "servingSize"]
      }
    }
  });
  return parseGeminiJson(response.text);
}

// Fix: generateGoalVisualization for high-quality image generation
export async function generateGoalVisualization(prompt: string, context: string): Promise<string | null> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  const response = await ai.models.generateContent({
    model: 'gemini-3-pro-image-preview',
    contents: {
      parts: [{ text: `High-quality, realistic fitness transformation vision: ${prompt}. User Context: ${context}. Cinematic lighting, professional bodybuilding aesthetic.` }],
    },
    config: {
      imageConfig: { aspectRatio: "3:4", imageSize: "1K" }
    }
  });
  
  for (const part of response.candidates[0].content.parts) {
    if (part.inlineData) {
      return `data:image/png;base64,${part.inlineData.data}`;
    }
  }
  return null;
}

// Fix: findEliteResources using Maps Grounding with Gemini 2.5
export async function findEliteResources(query: string, latitude: number, longitude: number): Promise<any> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  const response = await ai.models.generateContent({
    model: "gemini-2.5-flash",
    contents: `Find elite fitness or nutrition resources for: ${query}. Focus on locations near the user.`,
    config: {
      tools: [{ googleMaps: {} }],
      toolConfig: {
        retrievalConfig: {
          latLng: {
            latitude: latitude,
            longitude: longitude
          }
        }
      }
    },
  });
  return { 
    text: response.text || "Scanning local area...", 
    sources: response.candidates?.[0]?.groundingMetadata?.groundingChunks || [] 
  };
}

export function encodePCM(data: Float32Array): string {
  const l = data.length;
  const int16 = new Int16Array(l);
  for (let i = 0; i < l; i++) { int16[i] = data[i] * 32768; }
  let binary = '';
  const bytes = new Uint8Array(int16.buffer);
  for (let i = 0; i < bytes.byteLength; i++) { binary += String.fromCharCode(bytes[i]); }
  return btoa(binary);
}

export function decodeBase64(base64: string): Uint8Array {
  const binaryString = atob(base64);
  const bytes = new Uint8Array(binaryString.length);
  for (let i = 0; i < binaryString.length; i++) { bytes[i] = binaryString.charCodeAt(i); }
  return bytes;
}

export async function decodeAudioData(data: Uint8Array, ctx: AudioContext, sampleRate: number, numChannels: number): Promise<AudioBuffer> {
  const dataInt16 = new Int16Array(data.buffer);
  const frameCount = dataInt16.length / numChannels;
  const buffer = ctx.createBuffer(numChannels, frameCount, sampleRate);
  for (let channel = 0; channel < numChannels; channel++) {
    const channelData = buffer.getChannelData(channel);
    for (let i = 0; i < frameCount; i++) {
      channelData[i] = dataInt16[i * numChannels + channel] / 32768.0;
    }
  }
  return buffer;
}

export async function connectLiveCoach(callbacks: any) {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  return ai.live.connect({
    model: 'gemini-2.5-flash-native-audio-preview-12-2025',
    callbacks,
    config: {
      responseModalities: [Modality.AUDIO],
      speechConfig: { voiceConfig: { prebuiltVoiceConfig: { voiceName: 'Zephyr' } } },
      systemInstruction: "You are the MUSKYFIT coach. Speak in UK English. Be clear, professional, and encouraging."
    }
  });
}

export async function searchUniversalFood(query: string): Promise<any> {
  const ai = new GoogleGenAI({ apiKey: process.env.API_KEY });
  const response = await ai.models.generateContent({
    model: "gemini-3-flash-preview",
    contents: `Nutrition for: ${query}`,
    config: { 
      responseMimeType: "application/json", 
      responseSchema: { type: Type.OBJECT, properties: { name: { type: Type.STRING }, calories: { type: Type.NUMBER }, protein: { type: Type.NUMBER }, carbs: { type: Type.NUMBER }, fats: { type: Type.NUMBER }, servingSize: { type: Type.STRING } } } 
    }
  });
  return parseGeminiJson(response.text);
}
